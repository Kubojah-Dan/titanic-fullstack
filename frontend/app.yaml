runtime: nodejs18
handlers:
  - url: /
    static_files: build/index.html
    upload: build/index.html
  - url: /.*
    static_dir: build
env_variables:
  REACT_APP_API_URL: "https://titanic-backend-xxx.a.run.app"  # Update with your backend URL after deploying it